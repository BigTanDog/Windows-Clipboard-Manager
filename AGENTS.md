# AGENTS.md

本文件定义本项目的工程约束，所有改动都必须遵守。

## 1. Git 提交

每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
提交信息需说明做了什么改动、为什么这么改，不要只写"update"或"fix"。

## 2. 测试与验证

每次改动后，都必须编写或更新相关测试，并且在交付给用户前，确保所有测试和验证全部通过。
仅保证"能编译通过"不算验证完成——必须实际运行程序确认功能正常。

## 3. 剪贴板线程约束（重要）

剪贴板 API 有线程亲和性要求，必须在所属的 STA 线程上访问。

- 禁止使用 Task.Run、ThreadPool 或任何后台线程读取剪贴板。
- 接收 WM_CLIPBOARDUPDATE 消息的窗口线程必须保持 STA。
- UI 线程同样必须为 STA。
- 若需要把剪贴板数据交给后台处理，只传递已拷贝到托管内存的纯数据对象，
  绝对不要把任何 Win32 句柄或剪贴板引用跨线程传递。

## 4. 禁止联网（重要）

本工具会记录用户的剪贴板内容，属于高度敏感数据。

- 禁止添加任何联网功能：统计上报、检查更新、崩溃上传、遥测，一律不做。
- 禁止在日志中输出完整的剪贴板文本内容（最多记录前 50 字符用于调试）。
- 数据只能存放在程序目录下的 data/ 内，不得写入 %APPDATA% 或注册表
  （开机自启项除外）。

## 5. 禁止用删功能来让测试通过（重要）

- 严禁删除、注释、跳过或禁用任何现有功能，仅仅为了让测试或构建通过。
- 严禁为了让测试变绿而放宽断言、加 [Ignore] 标记或改测试预期。
- 若某功能确实有问题，先说明问题本身和你的判断依据，等用户决定。

## 6. 代码安全审查

每次改动完成后，必须对新增和修改的代码做一次安全审查，重点检查：

- **缓冲区与内存**：Win32 调用涉及的所有缓冲区长度计算是否正确，
  是否存在整数溢出、越界读写、分配大小与拷贝大小不一致。
- **句柄与资源泄露**：所有 HGLOBAL / HBITMAP / HANDLE / IStream 是否
  都在 try/finally 中释放，异常路径下是否会泄露。
- **路径处理**：CF_HDROP 读出的路径必须做长度校验与非法字符校验，
  禁止直接拼接路径字符串，需使用 Path.Combine 等安全 API。
- **解析逻辑**：HTML Format 头部偏移量解析必须做边界校验，
  防止畸形数据导致越界访问或死循环。
- **输入信任**：剪贴板内容来自任意第三方程序，一律视为不可信输入，
  所有解析都必须有长度上限和异常兜底。

审查发现问题必须当场修复，无法确定的要在交付时明确说明。
