# Windows 剪贴板管理器 —— 开发需求说明书

> 本文档用于交付给 AI 编程 Agent 作为开发依据。文档已自包含，阅读者无需任何额外上下文。

---

## 一、项目目标

开发一款运行在 **Windows 11** 上的**剪贴板历史管理工具**。核心定位：

| 维度 | 要求 |
|---|---|
| 体积 | 安装包或便携包 **≤ 25MB**，目标 15MB |
| 便携性 | **完全便携**，可放 U 盘运行，数据跟随程序目录 |
| 响应速度 | 冷启动 ≤ 500ms，面板弹出 ≤ 100ms |
| 常驻内存 | 空闲态 ≤ 80MB |
| 使用方式 | 常驻托盘，全局快捷键一键唤出，点击即用 |

**一句话描述**：一个体积小巧、随插随用、按下快捷键就立刻弹出的剪贴板历史面板，能记住你复制过的文字、富文本、图片、截图和文件路径，并且重启电脑也不丢。

---

## 二、技术栈（已确定，不要更改）

- **语言 / 运行时**：C# / .NET 8
- **UI 框架**：**首选 WPF**。若评估后认为 WinUI 3 在 AOT 裁剪与便携性上更优，可改用 WinUI 3，但必须在开工前说明理由。
- **发布方式**：`PublishSingleFile` + `PublishTrimmed`（或 NativeAOT），产出**单文件绿色版**。目标体积 15-25MB。
- **数据库**：SQLite（推荐 `Microsoft.Data.Sqlite`，轻量、无需额外部署）
- **Win32 互操作**：P/Invoke 调用 `user32.dll` / `shell32.dll` / `gdi32.dll`。推荐引入 `CsWin32`（微软官方 Source Generator）自动生成互操作签名，减少手写 P/Invoke 出错概率。
- **图标提取**：`shell32.dll` 的 `SHGetFileInfo`，或 `System.Drawing.Common`（注意 AOT 兼容性，如冲突则改用 Win32 原生方案）。
- **UI 风格**：Win11 原生 Fluent 观感（圆角、亚克力或 Mica 背景、跟随系统深浅色），但**列表采用紧凑工具风**（参考 Ditto 的信息密度：一行一条，左侧类型图标，右侧时间）。

---

## 三、功能需求

### 3.1 剪贴板监听（核心）

**不要用轮询。** 必须使用 Windows 官方事件机制：

1. 程序启动时创建一个**隐藏消息窗口**（`HWND_MESSAGE` 类型，仅用于接收消息，永不显示）。
2. 调用 `AddClipboardFormatListener(hwnd)` 注册监听。
3. 系统剪贴板内容变化时，会向该窗口投递 `WM_CLIPBOARDUPDATE` 消息（值 `0x031D`）。
4. 在窗口过程中处理该消息，读取剪贴板内容。
5. 程序退出时调用 `RemoveClipboardFormatListener(hwnd)` 注销。

**注意**：该消息窗口必须独立于主 UI 窗口存在，因为主窗口可能被隐藏或销毁，而监听需要 7×24 小时持续工作。

### 3.2 支持的内容类型

按以下顺序探测剪贴板格式，**取优先级最高的可用格式**保存（一次复制往往同时存在多种格式）：

| 优先级 | 内容类型 | 剪贴板格式常量 | 读取方式与说明 |
|---|---|---|---|
| 1 | 文件路径 | `CF_HDROP` (15) | 用 `DragQueryFile` 逐个取出路径。**只存路径字符串**，绝对不要读取文件内容。 |
| 2 | 图片 / 截图 | `CF_DIB` (8) / `CF_DIBV5` (17) | 转成 PNG 后存为独立文件。系统截图（Win+Shift+S）走的也是这个格式。 |
| 3 | 富文本 | `"HTML Format"`（注册格式，需 `RegisterClipboardFormat` 获取 ID） | 数据头部是 `Version:0.9\r\nStartHTML:...\r\nEndHTML:...` 格式的偏移量说明，需按 `StartHTML`/`EndHTML` 偏移截取真正的 HTML 片段。 |
| 4 | 纯文本 | `CF_UNICODETEXT` (13) | 直接读 UTF-16 字符串。 |

**关于文件内容的明确决策**：**只记录文件路径，绝不读取或缓存文件内容。** 原因：`CF_HDROP` 本身只提供路径；若要读内容需走 `CF_FILECONTENTS` 的 `IStream` 惰性流，大文件会瞬间占满内存和磁盘；且用户绝大多数场景只需要路径本身。可以额外提供一个「收藏文件内容」的手动操作按钮作为例外，但必须是用户显式触发。

### 3.3 记录管理

- 支持记录**多条**历史，默认可配置上限：**50 条 / 100 条 / 200 条 / 500 条 / 无限制**，默认 **100 条**。
- 相同内容**去重**：若新内容与已有记录完全相同（按内容哈希比较），则把已有记录提到最前并更新时间戳，不新增。
- 支持**手动删除**单条记录。
- 支持**一键清空**全部历史（需二次确认）。
- 支持**收藏 / 固定**记录，收藏项**永不参与自动淘汰**。
- **持久化**：退出、关机、重启后记录不丢失。
- **淘汰策略**：超出上限时优先删除「最旧 + 非收藏」的记录；若同时存在体积较大的记录（如大图），可优先删除大体积记录以控制磁盘占用。

### 3.4 主面板交互

- **全局快捷键**唤出面板，默认 `Ctrl+Shift+V`（可配置）。用 `RegisterHotKey` 注册；**注册失败必须检测并提示用户换键**（可能被其他软件占用）。
- 面板弹出位置：**鼠标所在屏幕的光标附近**（多显示器场景下需正确识别所在屏幕），或居中于当前活动窗口。
- 面板窗口必须设置：
  - `WS_EX_TOOLWINDOW`：不在 Alt+Tab 中出现
  - `WS_EX_NOACTIVATE` 或配合焦点管理：避免抢焦点导致奇怪行为
  - `TopMost`：始终置顶
- 监听 `WM_KILLFOCUS`：**点击面板外部时自动隐藏**，无需用户额外操作。
- 列表项显示：类型图标 + 内容摘要（文字取前 N 字、图片显示缩略图、文件显示文件名）+ 相对时间。
- 键盘操作：`↑`/`↓` 选择、`Enter` 粘贴到先前活动窗口、`Delete` 删除、`Esc` 关闭。
- 鼠标操作：**单击即粘贴**（这是核心体验，不要做成双击）。
- 搜索框：支持不区分大小写的模糊搜索（文字内容 + 文件名均可搜）。
- 粘贴时不只是写回剪贴板，还要**模拟 `Ctrl+V` 发送给先前的前台窗口**（可用 `SendInput` 实现，或简单方案：写回剪贴板后由用户手动粘贴，做成可选项）。

### 3.5 托盘与设置

- 系统托盘常驻图标，右键菜单：显示面板 / 设置 / 清空历史 / 开机自启 / 退出。
- **开机自启**：写 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`（**不要写 HKLM，避免需要管理员权限**）。可选延迟启动，避免拖慢开机。
- 设置项：记录条数上限、快捷键、是否开机自启、排除的应用（某些密码管理器会标记剪贴板内容，应跳过）、是否记录图片。
- 设置面板用 WPF 原生窗口即可，不必单独设计复杂架构。

### 3.6 明确不做的功能（第一版）

- 局域网 / 云同步
- 剪贴板内容的自动分类和 AI 处理
- 宏替换、脚本扩展
- 文件内容缓存
- 插件体系

---

## 四、数据存储设计

### 4.1 必须遵守的原则

1. **数据目录放在程序所在目录**，不要放 `%APPDATA%`。用 `AppContext.BaseDirectory` 定位，这个目录下建 `data/` 子目录。否则"便携"就名存实亡。
2. **不要把图片二进制或大段文本直接塞进数据库单表**。否则删一条记录要重写整个文件，数据量一上来就卡。
3. 元数据与二进制分离。

### 4.2 目录结构

```
<程序目录>/
├─ ClipboardManager.exe      # 主程序（单文件发布）
└─ data/
   ├─ history.db             # SQLite 数据库，只存元数据与索引
   ├─ blobs/                 # 图片、大段 HTML 的本体文件，按内容哈希命名（天然去重）
   └─ pinned/                # 用户收藏的内容本体
```

### 4.3 数据库表设计（建议）

**`clip_items` 表**

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK | 自增主键 |
| `type` | INTEGER | 内容类型：1=文本 2=HTML 3=图片 4=文件路径 |
| `text_content` | TEXT | 纯文本内容（文本类型直接存；其他类型存用于搜索的摘要） |
| `blob_path` | TEXT | 二进制本体在 `blobs/` 下的相对路径（图片/HTML 用），可为空 |
| `file_paths` | TEXT | 文件路径列表，多条用换行分隔（文件类型用） |
| `preview` | TEXT | 列表展示用的摘要文字（限长，建议 200 字符） |
| `content_hash` | TEXT | 内容 SHA256，用于去重，建唯一索引 |
| `size_bytes` | INTEGER | 内容体积，用于淘汰策略排序 |
| `is_pinned` | INTEGER | 是否收藏，0/1 |
| `source_app` | TEXT | 来源进程名（可选，如能获取到） |
| `created_at` | INTEGER | 首次记录时间戳（Unix 秒） |
| `updated_at` | INTEGER | 最近一次复制时间戳（去重时更新） |

**索引**：`content_hash` 唯一索引、`updated_at` 降序索引、`is_pinned` 索引。

**搜索**：优先用 SQLite `LIKE` 做模糊匹配（数据量在千条级别完全够用）。若后续需要更强的中文分词搜索，再考虑 FTS5，第一版不引入。

---

## 五、必须避开的坑（重要，很容易踩）

### 5.1 剪贴板自循环

你写回剪贴板时会**再次触发** `WM_CLIPBOARDUPDATE`，导致把自己的粘贴操作也记成一条新记录，形成循环。**必须加一个内部标志位**（如 `_isInternalWrite`），在自己写剪贴板期间跳过记录逻辑。这是 100% 会踩的坑。

### 5.2 `OpenClipboard` 可能失败

其他程序可能正持有剪贴板锁（常见于浏览器、Office）。`OpenClipboard` 返回失败（`ERROR_ACCESS_DENIED`）时必须**重试**，建议重试 3-5 次、每次间隔 30-50ms。否则会漏记录。

### 5.3 持有剪贴板锁时不要做慢操作

剪贴板是全局独占资源，你持有锁期间所有其他程序读不到剪贴板。**读完内容立刻 `CloseClipboard()`**。绝对不要在持有锁的时候做磁盘 IO、图片编码、UI 更新等耗时操作。正确做法：在锁内只把原始数据拷到内存，关锁后再做后续处理。

### 5.4 HTML 格式的数据头

`"HTML Format"` 的数据**不是纯 HTML**，开头有一段固定格式的头部说明（`Version` / `StartHTML` / `EndHTML` / `StartFragment` / `EndFragment` 五个字段）。必须解析这些偏移量，按 `StartFragment` 到 `EndFragment` 截取真正内容，否则存下来是一堆乱码头。

### 5.5 便携 = 不写注册表以外的位置

除了**开机自启必须写注册表**（`HKCU\...\Run`）之外，其他所有状态（设置、数据）都应写入程序目录下的 `data/`。同时注意：**程序放在 `Program Files` 下时目录不可写**，需检测写入失败并给出清晰提示（便携程序一般不会放那里，但要有兜底）。

### 5.6 单文件发布的 `AppContext.BaseDirectory`

单文件发布时 `Assembly.Location` 返回空字符串，**必须用 `AppContext.BaseDirectory`** 来定位程序目录。

### 5.7 高 DPI 与多显示器

Win11 缩放比例五花八门，务必配置好 DPI 感知（`app.manifest` 里声明 PerMonitorV2），并正确处理面板在非主显示器上的坐标换算。

### 5.8 AOT 兼容性

`NativeAOT` / `PublishTrimmed` 会剔除反射用到的类型。若使用 `System.Text.Json` 序列化，必须配置 `JsonSerializerContext` 源生成器。WPF **不完全支持 NativeAOT**，若选了 WPF，发布方式建议用 `PublishSingleFile` + `PublishTrimmed`（而非 NativeAOT），并做好体积预期管理（WPF 裁剪后约 30-50MB，若无法达标需向用户说明取舍）。

---

## 六、参考实现（用于降低开发成本）

开发前建议先研究这几款成熟软件：

| 软件 | 参考价值 |
|---|---|
| **Ditto**（开源，C++，约 5MB） | **最重要参考**。格式支持、去重逻辑、UI 信息密度、收藏机制均可借鉴。源码中剪贴板处理部分可直接阅读学习。 |
| **CopyQ**（开源，C++/Qt） | 快捷键设计、命令体系、设置项划分 |
| **Windows 自带剪贴板**（`Win+V`） | 系统级实现，本身就用了 `AddClipboardFormatListener` 这套 API。可用它对照验证自己监听是否正确。注意：系统历史默认只存 25 条且重启即清，且数据是私有二进制格式，**不要试图解析它的数据文件**，那不值得。 |
| **ClipboardFusion**（商业） | 触发规则与内容过滤的交互设计 |

---

## 七、建议的开发顺序

分三个阶段，每阶段都要能独立跑起来验证：

**阶段一：最小可用闭环**
- 隐藏消息窗口 + `AddClipboardFormatListener` 监听
- 只处理 `CF_UNICODETEXT`
- SQLite 落盘（单表，先不做 blobs 分离）
- 全局快捷键 + 面板弹出 + 列表展示 + 点击粘贴

> 本阶段最难的是**窗口消息循环**和**剪贴板自循环**两个问题，先集中精力解决它们，不要贪多。

**阶段二：多格式支持**
- 加入 `CF_DIB`（图片）与 `CF_HDROP`（文件路径）
- 加入 `"HTML Format"`（富文本）
- 引入 `blobs/` 目录分离存储 + 内容哈希去重
- 缩略图生成与缓存
- 搜索功能

**阶段三：体验打磨**
- 收藏 / 固定
- 淘汰策略
- 设置界面（条数上限、快捷键、开机自启、排除应用）
- 托盘图标与菜单
- 深浅色主题跟随
- 打包体积优化

---

## 八、验收标准

1. 冷启动时间 ≤ 500ms（在 SSD 上实测）。
2. 按下快捷键后，面板出现延迟 ≤ 100ms。
3. 连续复制 20 条不同类型内容（文字 / 网页富文本 / 截图 / 文件），全部正确记录且类型识别无误。
4. 电脑重启后，历史记录完整保留。
5. 程序放在 U 盘上换一台电脑运行，数据可正常读取（便携性验证）。
6. 用本工具粘贴内容后，**不会**产生重复的历史记录（自循环验证）。
7. 在其他程序长时间占用剪贴板的情况下，仍能正确捕获后续的复制操作（重试机制验证）。
8. 点击面板外部区域，面板自动隐藏。
9. 数据目录确实位于程序目录下的 `data/` 内（便携性验证）。
10. 最终产物体积符合预期，若因框架限制超出 25MB，需提供说明与可选方案。

---

## 九、交付物

1. 完整的 Visual Studio 解决方案（或可 `dotnet build` 直接构建的源码树）
2. 可运行的便携版发布产物
3. 简短 README：如何构建、如何发布、数据存放在哪里、已知限制
